function CardTree(id = 1, level = 0, type = 0, isHidden = false, parent = []) {
    this.id = id
    this.level = level
    this.type = type
    this.isHidden = isHidden
    this.parent = parent

    function removeParent() {

    }
}